"""Standalone copy of the Vector Lens engine.

Runs independently as `python -m lens_standalone` and can also be imported
back into the Vector app — the app supplies its own DataStore, so the
data_shim and runner modules stay inactive in that case.
"""
